/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { Video } from "./Video";
import { VideoCountArgs } from "./VideoCountArgs";
import { VideoFindManyArgs } from "./VideoFindManyArgs";
import { VideoFindUniqueArgs } from "./VideoFindUniqueArgs";
import { CreateVideoArgs } from "./CreateVideoArgs";
import { UpdateVideoArgs } from "./UpdateVideoArgs";
import { DeleteVideoArgs } from "./DeleteVideoArgs";
import { ScheduleFindManyArgs } from "../../schedule/base/ScheduleFindManyArgs";
import { Schedule } from "../../schedule/base/Schedule";
import { VideoDetailsFindManyArgs } from "../../videoDetails/base/VideoDetailsFindManyArgs";
import { VideoDetails } from "../../videoDetails/base/VideoDetails";
import { VideoDto } from "../VideoDto";
import { ExcelFileDto } from "../ExcelFileDto";
import { UpdateVideoDetailsDto } from "../UpdateVideoDetailsDto";
import { UpdateResultDto } from "../UpdateResultDto";
import { UploadExcelFileDto } from "../UploadExcelFileDto";
import { UploadResultDto } from "../UploadResultDto";
import { VideoService } from "../video.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => Video)
export class VideoResolverBase {
  constructor(
    protected readonly service: VideoService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "Video",
    action: "read",
    possession: "any",
  })
  async _videosMeta(
    @graphql.Args() args: VideoCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [Video])
  @nestAccessControl.UseRoles({
    resource: "Video",
    action: "read",
    possession: "any",
  })
  async videos(@graphql.Args() args: VideoFindManyArgs): Promise<Video[]> {
    return this.service.videos(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => Video, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "Video",
    action: "read",
    possession: "own",
  })
  async video(
    @graphql.Args() args: VideoFindUniqueArgs
  ): Promise<Video | null> {
    const result = await this.service.video(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => Video)
  @nestAccessControl.UseRoles({
    resource: "Video",
    action: "create",
    possession: "any",
  })
  async createVideo(@graphql.Args() args: CreateVideoArgs): Promise<Video> {
    return await this.service.createVideo({
      ...args,
      data: args.data,
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => Video)
  @nestAccessControl.UseRoles({
    resource: "Video",
    action: "update",
    possession: "any",
  })
  async updateVideo(
    @graphql.Args() args: UpdateVideoArgs
  ): Promise<Video | null> {
    try {
      return await this.service.updateVideo({
        ...args,
        data: args.data,
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => Video)
  @nestAccessControl.UseRoles({
    resource: "Video",
    action: "delete",
    possession: "any",
  })
  async deleteVideo(
    @graphql.Args() args: DeleteVideoArgs
  ): Promise<Video | null> {
    try {
      return await this.service.deleteVideo(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [Schedule], { name: "schedules" })
  @nestAccessControl.UseRoles({
    resource: "Schedule",
    action: "read",
    possession: "any",
  })
  async findSchedules(
    @graphql.Parent() parent: Video,
    @graphql.Args() args: ScheduleFindManyArgs
  ): Promise<Schedule[]> {
    const results = await this.service.findSchedules(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [VideoDetails], { name: "videoDetailsItems" })
  @nestAccessControl.UseRoles({
    resource: "VideoDetails",
    action: "read",
    possession: "any",
  })
  async findVideoDetailsItems(
    @graphql.Parent() parent: Video,
    @graphql.Args() args: VideoDetailsFindManyArgs
  ): Promise<VideoDetails[]> {
    const results = await this.service.findVideoDetailsItems(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @graphql.Query(() => ExcelFileDto)
  async DownloadExcelFile(
    @graphql.Args()
    args: VideoDto
  ): Promise<ExcelFileDto> {
    return this.service.DownloadExcelFile(args);
  }

  @graphql.Query(() => [VideoDto])
  async FetchYouTubeVideos(
    @graphql.Args()
    args: VideoDto
  ): Promise<VideoDto[]> {
    return this.service.FetchYouTubeVideos(args);
  }

  @graphql.Mutation(() => UpdateResultDto)
  async UpdateVideoDetailsViaExcelFile(
    @graphql.Args()
    args: UpdateVideoDetailsDto
  ): Promise<UpdateResultDto> {
    return this.service.UpdateVideoDetailsViaExcelFile(args);
  }

  @graphql.Mutation(() => UploadResultDto)
  async UploadExcelFile(
    @graphql.Args()
    args: UploadExcelFileDto
  ): Promise<UploadResultDto> {
    return this.service.UploadExcelFile(args);
  }
}
